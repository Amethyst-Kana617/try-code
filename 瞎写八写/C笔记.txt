int double_list[2][4] ={{1, 2, 3, 4}, {5, 6, 7, 8}};
for
(int i=0; i<2; i++){
    for(int j=0; j<4; j++){
        printf("%d\n",double_list[i][j]);
    }
}
其中的[i][j]指代的是[2][4]。运行二维数组的行下标0、1，列下标0、1、2、3。
for嵌套执行顺序是从外到里再从里到外。

for循环和while循环区别在一个先判断条件再执行，一个先执行再判断条件。